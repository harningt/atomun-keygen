language: java

sudo: false

jdk:
    - openjdk7

cache:
    directories:
        - $HOME/.m2
        - $HOME/.gradle/wrapper

script:
    - TERM=dumb ./gradlew clean check

notifications:
    webhooks:
        urls:
            - https://webhooks.gitter.im/e/74683ef1b91adc4b3534
        on_success: change
        on_failure: always
        on_start: false
